node_modules/

# Built vendor bundles (regenerated by `npm run build`)
vendor/marked.min.js
vendor/hljs.min.js

# Packaged extension artifacts
markitdown-*.zip
*.crx

# Extension signing key — NEVER commit. Loss of secrecy = anyone can publish
# updates signed as if they came from us.
*.pem

# Windows/WSL noise
*:Zone.Identifier

# Claude Code local settings
.claude/
